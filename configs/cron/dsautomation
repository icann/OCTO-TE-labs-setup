# update zone from DS files
*   * * * *   root   /root/scripts/dsautomation/dsupdate.pl %DOMAIN% 2>&1 | logger

# converting CDS record files to AWS format
*   * * * *   root   /root/scripts/dsautomation/cdsupdate.pl %DOMAIN% 2>&1 | logger

# Scanning for CDS records for all deployed groups
*/2 * * * *   root   /root/scripts/dsautomation/getcds.pl %DOMAIN% %NETWORKS% 2>&1 | logger

# going insecure
*   * * * *   root   /root/scripts/dsautomation/goinsecure.pl %DOMAIN% 2>&1 | logger

# last empty line is needed, otherwise cron will not accept this files
